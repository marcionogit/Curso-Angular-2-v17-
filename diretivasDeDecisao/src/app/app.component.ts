import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrl: './app.component.scss'
})
export class AppComponent {
  title = true;

  constructor(){
     
    // Diretivas de Decisão

    // QUANDO USAMOS:

    // ngIf usando complemento else, usando para exibir ou ocultar elementos tomado por decisões lógicas.

    // ngSwitch 
  }
}
